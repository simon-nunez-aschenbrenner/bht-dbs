LineChartPCM
0
Täglicher Anteil von Suchen zum Thema "Vogelgrippe"
Datum
pcm (1/100.000)
SELECT TO_CHAR(ZEITPUNKT, 'MM.DD'), COUNT(DISTINCT ZEITPUNKT) FROM H5N1_SUCHE GROUP BY TO_CHAR(ZEITPUNKT, 'MM.DD') ORDER BY 1
SELECT TO_CHAR(QUERYTIME, 'MM.DD'), COUNT(DISTINCT QUERYTIME) from AOLDATA.QUERYDATA GROUP BY TO_CHAR(QUERYTIME, 'MM.DD') ORDER BY 1