CREATE VIEW FOLGESUCHE_UEBERSICHT(THEMA, ANZAHL) AS SELECT NAME, COUNT(DISTINCT NUTZER_IN) FROM (SELECT * FROM FOLGESUCHE JOIN FOLGE_BEGRIFF ON FOLGESUCHE.BEGRIFF = FOLGE_BEGRIFF.BEGRIFF) JOIN FOLGE_KATEGORIE ON KATEGORIE_ID = FOLGE_KATEGORIE.ID GROUP BY NAME